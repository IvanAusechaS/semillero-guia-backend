# Variables de entorno para desarrollo
NODE_ENV=development
PORT=4000

# Base de datos SQLite (archivo local)
DB_DIALECT=sqlite
DB_STORAGE=database.sqlite
# La base de datos se creará automáticamente en la raíz del proyecto backend

# JWT Secret (cambiar en producción)
JWT_SECRET=tu_jwt_secret_super_seguro_aqui_cambiar_en_produccion_123456789
JWT_EXPIRE=7d

# URL del cliente (frontend)
CLIENT_URL=http://localhost:3000

# Configuración de archivos
MAX_FILE_SIZE=5000000
ALLOWED_FILE_TYPES=application/pdf,image/jpeg,image/png,image/gif

# Email (para notificaciones - opcional)
EMAIL_FROM=semillero.guia@correounivalle.edu.co
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=
SMTP_PASSWORD=